.. _preview_features:

Preview Features
================

A preview feature is a component of |short_name| introduced to receive early feedback
from users.

The key properties of a preview feature are:
- It is off by default and must be explicitly enabled.

General information on what is a preview feature from a reference.

May provide also links to all preview feature pages.
